 + ($ ($ 1 + 1) == 2)
 + match (rust) (== "deux" (match '(1 2)
                     ('(1)   "un") 
                     ('(1 2) "deux")
                     ("default")
                  ))
 + read entier file
 + eval / parse
 + import (?require¿)