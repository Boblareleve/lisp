 + slice
 + lign character error info (using .str.arr ptr )
 + code stepping (hard)
 + optimize recursion
 + gc: ref counting
 + match (rust) (== "deux" (match '(1 2)
                  ('(1)   "un") 
                  ('(1 2) "deux")
                  ("default")
               ))
 + test [] (index)
 + see 'static Strb' if they are ok