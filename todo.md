 + slice
 + lign character error info (using .str.arr ptr )
 + code stepping (hard)
 + optimize recursion
 + gc: ref counting
    arena ?
 + match (rust) (== "deux" (match '(1 2)
                  ('(1)   "un") 
                  ('(1 2) "deux")
                  ("default")
               ))
